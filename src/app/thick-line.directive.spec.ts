import { ThickLineDirective } from './thick-line.directive';

describe('ThickLineDirective', () => {
  it('should create an instance', () => {
    const directive = new ThickLineDirective();
    expect(directive).toBeTruthy();
  });
});
